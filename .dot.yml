---
role:
  pkg:
    - tmux
    # - name: tmux
    #   os: ["!windows"]
    # - name: tmux
    #   type: apt-cyg
    #   os: ["windows"]
    - name: reattach-to-user-namespace
      os: ["darwin"]
  dir: .tmux
  link:
    - "tmux.conf:.tmux"
    - "tmux-*.conf:.tmux"
    - "tmux.$OS.conf:.tmux"
    - "colors:.tmux"
  line:
    ".tmux.conf": "source-file $HOME/.tmux/tmux.conf"
